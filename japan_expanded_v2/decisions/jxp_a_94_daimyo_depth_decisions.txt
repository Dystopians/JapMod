# Generated by build_daimyo_depth_runtime.py. Do not edit.
country_decisions = {
	jxp_a_rebuild_main_house = {
		major = yes
		potential = {
			jxp_is_daimyo_stage_trigger = yes
			NOT = { has_country_flag = jxp_a_house_reconstruction_used }
			OR = {
				tag = AKM
				tag = AKT
				tag = AMA
				tag = ASA
				tag = ASK
				tag = CBA
				tag = CSK
				tag = DTE
				tag = HJO
				tag = HSK
				tag = HTK
				tag = IKE
				tag = ISK
				tag = ITO
				tag = KKC
				tag = KNO
				tag = KTB
				tag = MAE
				tag = MRI
				tag = OGS
				tag = OTM
				tag = OUC
				tag = RFR
				tag = SBA
				tag = SHN
				tag = SMZ
				tag = SOO
				tag = STK
				tag = TKD
				tag = TKG
				tag = TKI
				tag = TTI
				tag = UES
				tag = UTN
				tag = YMN
			}
		}
		allow = {
			NOT = { num_of_cities = 6 }
			stability = 0
			treasury = 50
		}
		effect = {
			set_country_flag = jxp_a_house_reconstruction_used
			add_treasury = -50
			if = {
				limit = { tag = AKM }
				country_event = { id = jxp_daimyo_depth.1002 }
			}
			if = {
				limit = { tag = AKT }
				country_event = { id = jxp_daimyo_depth.1022 }
			}
			if = {
				limit = { tag = AMA }
				country_event = { id = jxp_daimyo_depth.1042 }
			}
			if = {
				limit = { tag = ASA }
				country_event = { id = jxp_daimyo_depth.1062 }
			}
			if = {
				limit = { tag = ASK }
				country_event = { id = jxp_daimyo_depth.1083 }
			}
			if = {
				limit = { tag = CBA }
				country_event = { id = jxp_daimyo_depth.1102 }
			}
			if = {
				limit = { tag = CSK }
				country_event = { id = jxp_daimyo_depth.1123 }
			}
			if = {
				limit = { tag = DTE }
				country_event = { id = jxp_daimyo_depth.1143 }
			}
			if = {
				limit = { tag = HJO }
				country_event = { id = jxp_daimyo_depth.1163 }
			}
			if = {
				limit = { tag = HSK }
				country_event = { id = jxp_daimyo_depth.1182 }
			}
			if = {
				limit = { tag = HTK }
				country_event = { id = jxp_daimyo_depth.1202 }
			}
			if = {
				limit = { tag = IKE }
				country_event = { id = jxp_daimyo_depth.1222 }
			}
			if = {
				limit = { tag = ISK }
				country_event = { id = jxp_daimyo_depth.1242 }
			}
			if = {
				limit = { tag = ITO }
				country_event = { id = jxp_daimyo_depth.1262 }
			}
			if = {
				limit = { tag = KKC }
				country_event = { id = jxp_daimyo_depth.1282 }
			}
			if = {
				limit = { tag = KNO }
				country_event = { id = jxp_daimyo_depth.1302 }
			}
			if = {
				limit = { tag = KTB }
				country_event = { id = jxp_daimyo_depth.1322 }
			}
			if = {
				limit = { tag = MAE }
				country_event = { id = jxp_daimyo_depth.1342 }
			}
			if = {
				limit = { tag = MRI }
				country_event = { id = jxp_daimyo_depth.1363 }
			}
			if = {
				limit = { tag = OGS }
				country_event = { id = jxp_daimyo_depth.1382 }
			}
			if = {
				limit = { tag = OTM }
				country_event = { id = jxp_daimyo_depth.1403 }
			}
			if = {
				limit = { tag = OUC }
				country_event = { id = jxp_daimyo_depth.1423 }
			}
			if = {
				limit = { tag = RFR }
				country_event = { id = jxp_daimyo_depth.1442 }
			}
			if = {
				limit = { tag = SBA }
				country_event = { id = jxp_daimyo_depth.1462 }
			}
			if = {
				limit = { tag = SHN }
				country_event = { id = jxp_daimyo_depth.1482 }
			}
			if = {
				limit = { tag = SMZ }
				country_event = { id = jxp_daimyo_depth.1503 }
			}
			if = {
				limit = { tag = SOO }
				country_event = { id = jxp_daimyo_depth.1523 }
			}
			if = {
				limit = { tag = STK }
				country_event = { id = jxp_daimyo_depth.1542 }
			}
			if = {
				limit = { tag = TKD }
				country_event = { id = jxp_daimyo_depth.1563 }
			}
			if = {
				limit = { tag = TKG }
				country_event = { id = jxp_daimyo_depth.1583 }
			}
			if = {
				limit = { tag = TKI }
				country_event = { id = jxp_daimyo_depth.1602 }
			}
			if = {
				limit = { tag = TTI }
				country_event = { id = jxp_daimyo_depth.1622 }
			}
			if = {
				limit = { tag = UES }
				country_event = { id = jxp_daimyo_depth.1643 }
			}
			if = {
				limit = { tag = UTN }
				country_event = { id = jxp_daimyo_depth.1662 }
			}
			if = {
				limit = { tag = YMN }
				country_event = { id = jxp_daimyo_depth.1682 }
			}
		}
		ai_will_do = { factor = 0.20 }
	}
}
