country_decisions = {
	jxp_b_decision_open_colonial_society_register = {
		major = yes

		potential = {
			jxp_b_91_colonial_society_entry_trigger = yes
			NOT = { has_country_flag = jxp_b_colonial_society_active }
		}

		allow = {
			is_at_war = no
			num_of_ports = 5
			dip_power = 75
			treasury = 200
		}

		effect = {
			add_dip_power = -75
			add_treasury = -200
			country_event = { id = jxp_colonial_society.1 days = 1 }
		}

		ai_will_do = {
			factor = 0.35
			modifier = { factor = 0 is_at_war = yes }
			modifier = { factor = 0 has_any_disaster = yes }
			modifier = { factor = 0 NOT = { treasury = 350 } }
			modifier = { factor = 2 has_country_flag = jxp_path_open_trade }
			modifier = { factor = 2 has_country_flag = jxp_iface_house_maritime_ready }
			modifier = { factor = 0.25 has_country_flag = jxp_path_sakoku }
		}
	}
}
